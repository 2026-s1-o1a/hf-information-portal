services:
  umb_database:
    container_name: umb_database
    build:
      context: ./Database
    environment:
      MSSQL_SA_PASSWORD: ${DB_PASSWORD}
      SA_PASSWORD: ${DB_PASSWORD}
    ports:
      - "1433:1433"
      - "1434:1434"
    volumes:
      - umb_database:/var/opt/mssql
    networks:
      - umbnet
    healthcheck:
      # This healthcheck is to make sure that the database is up and running before the umbraco container starts.
      # It works by querying the database for the state of the umbracoDb database, ensuring it exists.
      test: ["CMD", "bash", "/healthcheck.sh"]
      interval: 5m
      timeout: 5s
      retries: 3
      start_period: 30s # Bootstrap duration, for this duration failures does not count towards max retries.
      start_interval: 5s # How long after the health check has started to run the healthcheck again.
    
  umb_client:
    container_name: umb_client
    working_dir: /client
    build:
      context: ./client
      dockerfile: Dockerfile
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "3000:5173"
    volumes:
      - ./client:/client
      - /client/node_modules
    depends_on:
      - umb_database

  umb_server:
    container_name: umb_server
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8080:44378"


networks:
  umbnet:
    driver: bridge


volumes:
  umb_database: